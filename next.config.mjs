/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['raw.githubusercontent.com'], // Thêm hostname của hình ảnh vào đây
      },
};

export default nextConfig;
